name: Bootstrap project

on:
  push:
    paths:
      - bootstrap.ready

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Import project
        shell: bash
        run: |
          set -euo pipefail
          cat bootstrap.b64.part* > bootstrap.b64
          base64 --decode bootstrap.b64 > bootstrap.zip
          rm -rf .bootstrap
          mkdir .bootstrap
          unzip -q bootstrap.zip -d .bootstrap
          shopt -s dotglob nullglob
          cp -a .bootstrap/ajan-ordusu/. .
          rm -rf .bootstrap bootstrap.zip bootstrap.b64 bootstrap.b64.part* bootstrap.ready
          rm -f .github/workflows/bootstrap.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Import agent army project"
          git push
